<div class="container">
  <h2> Pedidos </h2>
  <div class="row">
    <div class="col-4 mt-3">
      <%= render "form", pedido: @pedido %>
    </div>
    <div class="col-8 mt-3">
      <% if @pedidos[0].nil?  %>
        <h4>Se quiser, você pode fazer seu <strong>primeiro pedido<strong> de acesso às notas fiscais: </h4>
      <% elsif !@pedidos[0].nil? %>
        <h4 class="m-0"> Pedidos já solicitados: </h4>
        <%= render "pedido", pedidos: @pedidos %>
      <% else %>
        <%= render "pedido", pedidos: @pedidos %>
      <% end %>
    </div>
  </div>
</div>
